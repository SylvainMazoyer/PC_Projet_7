name: Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: ${{secrets.1c8198ef-86b1-4ead-b40e-792e00db38fc}}
          heroku_app_name: "smprojet7" #Must be unique in Heroku
          heroku_email: "mazoyersylvain43@gmail.com"
